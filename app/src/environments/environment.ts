// Environment configuration for API usage
export const environment = {
  production: false,
  useMockData: false, // Use mock data - no backend server required
  apiUrl: 'http://localhost:3000/', // backend URL
};
